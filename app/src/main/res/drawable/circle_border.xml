<!-- circle_border.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@android:color/transparent" /> <!-- Change to the color you want -->
    <stroke
        android:width="5dp"
        android:color="@color/speech"/>
</shape>
